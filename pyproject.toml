[tool.poetry]
name = "sigla"
version = "0.0.2"
description = ""
authors = ["mg santos <mauro.goncalo@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
Jinja2 = "^2.11.2"
click = "^7.1.2"
pydash = "^4.9.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
greet = "sigla.ui.location:cli"
sigla = "sigla.main:cli"